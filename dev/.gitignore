# Generated by the build. `npm run build` rewrites everything here from scratch.
/build/

# Installed by `npm install`.
/node_modules/

# Python bytecode from importing paths.py.
__pycache__/

# A private vault variant (dev/<name>/ with its own variant.json) is yours, not
# the repo's. Add its directory here so it can never be committed by accident.
